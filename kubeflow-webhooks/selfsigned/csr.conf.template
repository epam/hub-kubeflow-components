[ req ]
default_bits = 2048
prompt = no
default_md = sha256
req_extensions = req_ext
distinguished_name = dn

[ dn ]
C = LV
ST = LV
L = Riga
O = EPAM
OU = ${component.kubeflow.name}
CN = ${dns.domain}

[ req_ext ]
subjectAltName = @alt_names

[ alt_names ]
DNS.1 = admission-webhook-service
DNS.2 = admission-webhook-service.${component.kubeflow.namespace}
DNS.3 = admission-webhook-service.${component.kubeflow.namespace}.svc
DNS.4 = admission-webhook-service.${component.kubeflow.namespace}.svc.cluster
DNS.5 = admission-webhook-service.${component.kubeflow.namespace}.svc.cluster.local

[ v3_ext ]
authorityKeyIdentifier=keyid,issuer:always
basicConstraints=CA:FALSE
keyUsage=keyEncipherment,dataEncipherment
extendedKeyUsage=serverAuth,clientAuth
subjectAltName=@alt_names
