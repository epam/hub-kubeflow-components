apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: "{{.kubernetes.namespace}}"

resources:
- kustomize/base
- kustomize/cluster-roles.yaml
- gateway.yaml

generatorOptions:
  disableNameSuffixHash: true
